services:
  shopping-assistant:
    build: .
    container_name: shopping-assistant
    ports:
      - "6010:6010"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:6010/healthz')"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
